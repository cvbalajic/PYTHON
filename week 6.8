n = int(input())
arr = []
for i in range(n):
    arr.append(int(input()))
freq = {}
for num in arr:
    if num in freq:
        freq[num] += 1
    else:
        freq[num] = 1
for key, value in freq.items():
    print(f"{key} occurs {value} times")
